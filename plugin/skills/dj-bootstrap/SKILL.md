---
description: "Deprecated: use /djs-bootstrap"
---

`/dj-bootstrap` is deprecated and will be removed in a future release; use
`/djs-bootstrap`. Tell the user this in one line, then follow
`../djs-bootstrap/SKILL.md` (relative to this skill's directory) with the same
`$ARGUMENTS`.
