---
description: "Deprecated: renamed to /dj-deploy-observe — runs it automatically"
---

**This command has been renamed to `/dj-deploy-observe`.**

Tell the user:

> `/dj-launch-observability` is deprecated and has been renamed to `/dj-deploy-observe`. Running `/dj-deploy-observe` now — please use the new name in future.

Then follow `.agents/skills/dj-deploy-observe/SKILL.md` exactly as written, passing through
any arguments. Behaviour is identical — do not ask the user to confirm the switch.
