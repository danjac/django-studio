---
description: "Deprecated: renamed to /djs-deploy-observe — runs it automatically"
---

**This command has been renamed to `/djs-deploy-observe`.**

Tell the user:

> `/djs-launch-observability` is deprecated and has been renamed to `/djs-deploy-observe`. Running `/djs-deploy-observe` now — please use the new name in future.

Then follow `.agents/skills/djs-deploy-observe/SKILL.md` exactly as written, passing through
any arguments. Behaviour is identical — do not ask the user to confirm the switch.
