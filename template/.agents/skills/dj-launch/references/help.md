**/dj-launch** — deprecated

Renamed to `/dj-deploy`. Running `/dj-launch` still works: it switches to
`/dj-deploy` automatically with identical behaviour. Use the new name.

See `/dj-help dj-deploy` for full documentation.

Example:
  /dj-deploy
