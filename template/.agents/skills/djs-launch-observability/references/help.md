**/djs-launch-observability** — deprecated

Renamed to `/djs-deploy-observe`. Running `/djs-launch-observability` still works: it
switches to `/djs-deploy-observe` automatically with identical behaviour. Use the new name.

See `/djs-help djs-deploy-observe` for full documentation.

Example:
  /djs-deploy-observe
