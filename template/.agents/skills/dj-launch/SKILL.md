---
description: "Deprecated: renamed to /dj-deploy — runs /dj-deploy automatically"
---

**This command has been renamed to `/dj-deploy`.**

Tell the user:

> `/dj-launch` is deprecated and has been renamed to `/dj-deploy`. Running `/dj-deploy` now — please use the new name in future.

Then follow `.agents/skills/dj-deploy/SKILL.md` exactly as written, passing through any
arguments. Behaviour is identical — do not ask the user to confirm the switch.
