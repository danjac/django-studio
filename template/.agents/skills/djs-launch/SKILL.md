---
description: "Deprecated: renamed to /djs-deploy — runs /djs-deploy automatically"
---

**This command has been renamed to `/djs-deploy`.**

Tell the user:

> `/djs-launch` is deprecated and has been renamed to `/djs-deploy`. Running `/djs-deploy` now — please use the new name in future.

Then follow `.agents/skills/djs-deploy/SKILL.md` exactly as written, passing through any
arguments. Behaviour is identical — do not ask the user to confirm the switch.
