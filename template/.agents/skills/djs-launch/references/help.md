**/djs-launch** — deprecated

Renamed to `/djs-deploy`. Running `/djs-launch` still works: it switches to
`/djs-deploy` automatically with identical behaviour. Use the new name.

See `/djs-help djs-deploy` for full documentation.

Example:
  /djs-deploy
